name: Discord Events Sync

on:
  schedule:
    # Baseline: roughly hourly. Offset to xx:07 (instead of xx:00) because
    # GitHub Actions cron is best-effort and queues are deepest at the top
    # of every hour — many repos use '0 * * * *' and runs there can lag
    # 20–40 min. Shifting a few minutes off the hour dramatically improves
    # on-time firing without changing the cadence.
    - cron: '7 * * * *'
    # Session burst: roughly every 10 minutes during the weekly Sunday play
    # session so freshly-posted events land on the live map within minutes
    # instead of up to an hour later. Offset off the *:00/:10/:20 grid
    # (same reason as above — those slots are the most congested) by using
    # 5,15,25,35,45,55 instead of */10.
    #
    # Schedule (UTC):  Sunday 18:00 → 22:59  (5 hours, fires at :05/:15/:25/:35/:45/:55)
    # Local (BST):     Sunday 19:00 → 23:59  (matches 4-hour 19:00 session
    #                                          + 1-hour buffer after end)
    #
    # If the session moves or your local timezone shifts (e.g. BST → GMT
    # in late October), edit the hour range below. Cron is always UTC.
    - cron: '5,15,25,35,45,55 18-22 * * 0'
  workflow_dispatch:
    inputs:
      since:
        description: 'One-shot backfill from this date (YYYY-MM-DD or ISO). Leave blank for normal cron resume.'
        required: false
        default: ''
      chunk_days:
        description: 'Optional: when set together with since, run the backfill in N-day chunks with a sleep between, to ease Discord rate-limit pressure on large windows. Each chunk commits incrementally. Try 3 or 5 first.'
        required: false
        default: ''
      chunk_pause_seconds:
        description: 'Seconds to sleep between chunks (default 10). Only used when chunk_days is set.'
        required: false
        default: '10'
      debug_fetch:
        description: 'Diagnostic: fetch a specific message and dump its raw content + regex match info. Paste a full Discord URL (.../channels/.../.../...) or channel_id:message_id. Leave blank for normal runs.'
        required: false
        default: ''
      backfill_channel_ids:
        description: 'One-shot: find the channel_id for any event missing one (older events that fell out of the event_meta cache). Walks all known threads + main channel. Commits when finished. No regular sync runs alongside this.'
        type: boolean
        required: false
        default: false
      reconcile_rejections:
        description: 'One-shot: re-evaluate every entry in rejected_meta for missed continuations (no per-run cap). Use after deploying changes to the merge logic to apply them retroactively to the existing backlog of rejected messages. Runs alongside the normal sync; commits when finished.'
        type: boolean
        required: false
        default: false
      backfill_attachments:
        description: 'One-shot: walk every event whose images[] lack a `local` field, refetch the Discord message (gets freshly-signed CDN URLs), and download the attachments to data/attachments/. Use to repair the backlog after Discord CDN URL rotation has nuked older URLs. Commits when finished. No regular sync runs alongside this.'
        type: boolean
        required: false
        default: false

# Serialise runs so the every-10-min burst can't double-fire (e.g. one run
# overruns its slot, the next slot fires while the first is still going).
# Newer runs wait for the older to finish; if multiple pile up, only the
# latest stays queued — fine here because each run is idempotent.
concurrency:
  group: discord-sync
  cancel-in-progress: false

jobs:
  sync:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install requests

      - name: Configure git
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Run Discord sync (single pass)
        if: inputs.chunk_days == '' && inputs.backfill_channel_ids != true && inputs.backfill_attachments != true
        id: sync
        env:
          DISCORD_TOKEN: ${{ secrets.DISCORD_TOKEN }}
          SYNC_SINCE: ${{ inputs.since }}
          DEBUG_FETCH: ${{ inputs.debug_fetch }}
          # Lifts the MAX_PROMOTION_CHECK_PER_RUN cap so a single run can chew
          # through the whole rejected_meta backlog. Used after deploying new
          # reconciliation logic to apply it retroactively.
          RECONCILE_REJECTIONS: ${{ inputs.reconcile_rejections == true && 'true' || '' }}
        # Per-campaign channel id + paths come from campaigns.json (--campaign).
        # To add another synced campaign, add a discord_sync block there and
        # either parameterize this workflow or fan out via matrix strategy.
        run: python tools/sync_events.py --campaign darthsunday

      - name: Commit and push (single pass)
        if: inputs.chunk_days == '' && inputs.backfill_channel_ids != true && inputs.backfill_attachments != true && steps.sync.outputs.had_changes == 'true'
        run: |
          # data/attachments/ pulled in because sync_events.py mirrors new
          # image attachments locally — see the "Mirroring image attachments"
          # log line. Files there are part of the commit so the viewer's
          # local-path-preferred image rendering survives Discord CDN
          # signature rotation.
          git add darthsunday/data/events.json darthsunday/data/processed_ids.json darthsunday/data/attachments
          git commit -m "chore: sync Discord events $(date -u +%Y-%m-%d\ %H:%M\ UTC)"
          git push

      # One-shot channel_id backfill — walks the guild's threads + main channel
      # trying to locate each events.json entry that's missing channel_id (older
      # events that fell out of the event_meta cache). Pairs with the
      # "View on Discord" link in the viewer.
      - name: Backfill channel_ids
        if: inputs.backfill_channel_ids == true
        id: backfill
        env:
          DISCORD_TOKEN: ${{ secrets.DISCORD_TOKEN }}
        run: python tools/backfill_channel_ids.py --campaign darthsunday

      - name: Commit and push (channel_id backfill)
        if: inputs.backfill_channel_ids == true && steps.backfill.outputs.had_changes == 'true'
        run: |
          git add darthsunday/data/events.json
          git commit -m "chore: backfill channel_ids on older events $(date -u +%Y-%m-%d\ %H:%M\ UTC)"
          git push

      # One-shot attachment backfill — refetches every event that still has
      # un-mirrored images (no `local` field, or local file missing), gets
      # freshly-signed Discord CDN URLs from the refetch, and downloads them
      # to darthsunday/data/attachments/. Run this once after deploying the
      # mirror feature to repair the existing backlog of expired-URL images;
      # the normal cron sync handles new attachments going forward.
      - name: Backfill attachments
        if: inputs.backfill_attachments == true
        id: backfill_attach
        env:
          DISCORD_TOKEN: ${{ secrets.DISCORD_TOKEN }}
        run: python tools/backfill_attachments.py --campaign darthsunday

      - name: Commit and push (attachment backfill)
        if: inputs.backfill_attachments == true && steps.backfill_attach.outputs.had_changes == 'true'
        run: |
          git add darthsunday/data/events.json darthsunday/data/attachments
          git commit -m "chore: backfill mirrored Discord attachments $(date -u +%Y-%m-%d\ %H:%M\ UTC)"
          git push

      # Chunked-backfill mode: walk from `since` to today in `chunk_days`
      # increments, calling the sync once per chunk with --since/--until and
      # committing+pushing between chunks. This keeps each chunk's Discord API
      # usage small enough to avoid global rate-limit churn on large backfills,
      # and lands progress incrementally so a later failure doesn't lose work.
      - name: Run Discord sync (chunked backfill)
        if: inputs.chunk_days != '' && inputs.backfill_channel_ids != true && inputs.backfill_attachments != true
        env:
          DISCORD_TOKEN: ${{ secrets.DISCORD_TOKEN }}
          SINCE: ${{ inputs.since }}
          CHUNK_DAYS: ${{ inputs.chunk_days }}
          CHUNK_PAUSE: ${{ inputs.chunk_pause_seconds }}
        run: |
          set -euo pipefail
          if [ -z "$SINCE" ]; then
            echo "ERROR: chunk_days set but since is empty — chunked mode needs a starting date."
            exit 1
          fi
          if ! [[ "$CHUNK_DAYS" =~ ^[0-9]+$ ]] || [ "$CHUNK_DAYS" -lt 1 ]; then
            echo "ERROR: chunk_days must be a positive integer, got $CHUNK_DAYS"
            exit 1
          fi

          TODAY=$(date -u +%Y-%m-%d)
          current="$SINCE"
          chunk_num=0
          while [ "$current" \< "$TODAY" ]; do
            chunk_num=$((chunk_num + 1))
            next=$(date -u -d "$current + $CHUNK_DAYS days" +%Y-%m-%d)
            if [ "$next" \> "$TODAY" ] || [ "$next" = "$TODAY" ]; then
              next="$TODAY"
            fi
            echo ""
            echo "════════════════════════════════════════════════════════"
            echo "  Chunk #$chunk_num: $current → $next"
            echo "════════════════════════════════════════════════════════"

            python tools/sync_events.py --campaign darthsunday \
              --since "$current" --until "$next"

            # Commit + push between chunks so progress survives a later failure.
            git add darthsunday/data/events.json darthsunday/data/processed_ids.json
            if ! git diff --cached --quiet; then
              git commit -m "chore: sync Discord events (chunk $current..$next)"
              git push
              echo "  committed + pushed chunk $chunk_num"
            else
              echo "  no changes in chunk $chunk_num"
            fi

            current="$next"
            if [ "$current" != "$TODAY" ]; then
              echo "  sleeping ${CHUNK_PAUSE}s to ease rate-limit pressure ..."
              sleep "$CHUNK_PAUSE"
            fi
          done
          echo ""
          echo "Chunked backfill complete: $chunk_num chunk(s) processed."